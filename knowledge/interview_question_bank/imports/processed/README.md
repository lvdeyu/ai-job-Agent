# 处理中间稿区

这里放从 `imports/raw/` 或外部文档中抽取、清洗后的中间结果。

常见文件包括：

- 抽取后的纯文本
- AI 初步拆分出的候选题目
- 待人工审核的 JSONL 草稿
- 去重和改写记录

这些文件仍然不是正式题库。只有审核通过、字段完整、版权风险可控的题目，才移动到 `seeds/`。

## 推荐流程

```text
raw 原始资料
  -> 文本抽取
  -> 问答切分
  -> 改写与补充评分 Rubric
  -> 人工抽查
  -> seeds 正式题库
```

